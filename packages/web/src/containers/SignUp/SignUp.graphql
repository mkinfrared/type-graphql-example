mutation RegisterUser(
  $email: String!
  $username: String!
  $password: String!
  $passwordConfirm: String!
) {
  register(
    email: $email
    username: $username
    password: $password
    passwordConfirm: $passwordConfirm
  ) {
    email
  }
}
